package it.polimi.se2019.view;

public enum SceneType {
    SELECT_NICKNAME,
    SELECT_CHARACTER,
    SELECT_SKULLS,
    SELECT_ARENA,
    LOBBY,
    CONNECTION_ERROR,
    RELOAD_GAME,
    INVALID_TOKEN
}